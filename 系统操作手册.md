# 数据要素×房屋建筑城建档案协同共享服务系统操作手册

> 系统版本：1.0.0  
> 文档版本：1.0  
> 更新日期：2026-07-27  
> 适用对象：城建档案管理人员、外部协同部门用户、系统运维人员

## 1. 系统简介

本系统用于房屋建筑城建档案的数据要素管理、服务组件协同、内部审批、运行监控和安全信息展示。

系统主要包含以下模块：

- 首页：显示功能入口和内部审批事项。
- 审批管理：新增、查询、查看、审批和删除内部审批记录。
- 数据要素管理：管理业务数据、业务元数据、服务实例数据和日志数据。
- 服务组件管理：完成服务注册、提交审批、发布、授权、配置和下架。
- 监控运维：查看运行指标、服务状态、调用趋势和事件日志。
- 安全防护：查看安全模块状态、安全趋势和安全事件。
- 设置：修改密码、设置登录页背景图片和退出系统。

## 2. 运行环境与访问方式

### 2.1 环境要求

| 项目 | 要求 |
| --- | --- |
| 操作系统 | macOS、Linux 或 Windows |
| Java | JDK 8 或更高版本 |
| Maven | 3.6 或更高版本 |
| 数据库 | MySQL，建议 MySQL 8.0 |
| 浏览器 | Chrome、Edge 等现代浏览器 |
| 默认端口 | 8080 |
| 单次上传上限 | 10 MB |

### 2.2 默认数据库配置

系统默认连接参数位于 `src/main/resources/application.yml`：

| 配置项 | 默认值 |
| --- | --- |
| 地址 | `localhost:3306` |
| 数据库 | `collaborative_sharing` |
| 用户名 | 环境变量 `DB_USERNAME`，默认 `root` |
| 密码 | 环境变量 `DB_PASSWORD`，无默认值 |
| 时区 | `Asia/Shanghai` |

启动前必须通过环境变量提供数据库密码，必要时也可通过 `DB_URL` 覆盖完整数据库连接地址。

### 2.3 一键启动

在项目根目录执行：

```bash
export DB_PASSWORD='你的MySQL密码'
chmod +x start.sh
./start.sh
```

启动脚本将依次检查 Java、Maven 和 MySQL，初始化数据库，编译并启动应用。macOS 环境下，如果已安装 Homebrew，脚本会尝试自动启动 MySQL；其他环境需先手动启动 MySQL。

看到以下提示后表示服务已启动：

```text
应用将在 http://localhost:8080 启动
```

### 2.4 手动启动

如果不使用启动脚本，可按以下顺序执行：

```bash
export DB_PASSWORD='你的MySQL密码'
mysql -u root -p < database-init.sql
mvn spring-boot:run
```

也可以先打包再运行：

```bash
mvn clean package
java -jar target/sharing-system-1.0.0.jar
```

为确保上传文件路径一致，请始终以项目根目录作为运行目录。

### 2.5 访问系统

本机访问地址：

```text
http://localhost:8080
```

局域网其他设备访问时，将 `localhost` 替换为服务器 IP，并确保防火墙已放行 8080 端口。

## 3. 用户角色与默认账号

### 3.1 角色权限

| 功能 | 档案管理员 | 外部部门 |
| --- | :---: | :---: |
| 首页 | ✓ | — |
| 内部审批管理 | ✓ | — |
| 数据要素管理 | ✓ | — |
| 查看服务组件 | ✓ | 限可见范围 |
| 注册服务 | ✓ | ✓ |
| 编辑、提交本部门服务 | ✓ | ✓ |
| 发布、授权、配置、下架服务 | ✓ | — |
| 监控运维 | ✓ | — |
| 安全防护 | ✓ | — |
| 修改本人密码 | ✓ | ✓ |
| 修改登录背景 | ✓ | — |

外部部门只能看到以下服务：

- 本部门创建的服务；
- 状态为“已发布”的服务；
- 状态为“已授权”且授权单位包含本部门名称的服务。

### 3.2 默认账号

| 角色 | 部门 | 用户名 | 初始密码 |
| --- | --- | --- | --- |
| 档案管理员 | 城建档案局 | `admin` | `admin` |
| 外部部门 | 物业办 | `wyb` | `123456` |
| 外部部门 | 质安科 | `zak` | `123456` |
| 外部部门 | 不动产 | `bdc` | `123456` |
| 外部部门 | 物业公司 | `wygs` | `123456` |
| 外部部门 | 应急办 | `yjb` | `123456` |
| 外部部门 | 公安系统 | `gaj` | `123456` |

首次登录后应立即修改密码。默认账号仅适合开发、演示和初始化使用。

## 4. 登录、导航与退出

### 4.1 登录

1. 打开系统地址。
2. 输入用户名和密码。
3. 可点击密码框右侧的眼睛图标显示或隐藏密码。
4. 点击“登录”。

登录失败时，页面会提示“用户名或密码错误”。

档案管理员登录后进入首页；外部部门登录后直接进入服务组件管理页面。

> 当前版本的“自动登录”和“忘记密码”仅显示在页面上，尚未接入实际功能。忘记密码时需由数据库管理员处理。

### 4.2 顶部导航

档案管理员可访问：首页、审批管理、数据要素管理、服务组件管理、监控运维、安全防护和设置。

“数据要素管理”是下拉菜单，包含：

- 业务数据；
- 业务元数据；
- 服务实例数据；
- 日志数据。

外部部门仅显示“服务组件管理”和“设置”。

### 4.3 退出登录

进入“设置”，在“账号操作”区域点击“退出登录”。退出后当前会话失效并返回登录页。

## 5. 首页

首页仅对档案管理员开放。

页面上方提供以下快捷入口：

- 数据要素管理；
- 服务组件管理；
- 监控运维；
- 安全防护。

页面下方显示内部审批事项及其状态。点击审批记录右侧的“查看”可进入审批详情。

## 6. 内部审批管理

内部审批管理仅对档案管理员开放，地址为 `/todo-items`。

### 6.1 查询审批记录

可按以下条件查询：

- 审批事项；
- 提交人；
- 申请部门；
- 状态：未审批、已通过或已拒绝。

点击“查询”执行筛选，点击“重置”清除筛选条件。列表默认每页显示 10 条记录。

### 6.2 新增内部审批

1. 点击“+ 新增内部审批”。
2. 填写审批事项。
3. 填写申请部门。
4. 填写审批说明。
5. 根据需要选择附件。
6. 点击“提交”。

提交后记录状态为“未审批”。

附件支持 PDF、Word、Excel、PowerPoint、TXT、常见图片、ZIP 和 RAR，单次请求总大小不得超过 10 MB。

### 6.3 查看和下载附件

1. 在列表中点击“查看详情”。
2. 查看提交人、申请部门、提交时间、审批说明和当前状态。
3. 如存在附件，点击“查看附件”下载。

### 6.4 审批

待审批记录会显示“审批操作”区域：

1. 输入审批意见。
2. 点击“通过”或“拒绝”。
3. 操作成功后，系统记录审批人、审批时间和审批意见。

审批意见不能为空。已通过或已拒绝的记录不再显示审批按钮。

### 6.5 删除审批记录

在列表中点击“删除”，确认后删除审批记录及其关联附件。该操作不可在页面中撤销，操作前应确认记录不再需要保留。

> 内部审批与服务组件状态流转是两个独立流程。审批记录通过后，不会自动发布或授权服务组件。

## 7. 数据要素管理

数据要素管理仅对档案管理员开放。

### 7.1 数据类型

| 一级类型 | 主要用途 |
| --- | --- |
| 业务数据 | 管理工程基本信息、图纸、合同、验收等城建档案业务记录 |
| 业务元数据 | 查看业务数据表头字段、数据库字段名和所属数据子类 |
| 服务实例数据 | 管理服务注册、版本、认证、授权、路由和上下架等实例数据 |
| 日志数据 | 管理系统运行、安全审计、访问调用、用户行为、健康监控和异常告警数据 |

业务数据默认包含 9 个子类：

1. 工程基本信息；
2. 单体工程信息；
3. 竣工图纸信息；
4. 工程合同信息；
5. 竣工验收信息；
6. 产权人信息；
7. 标准地名信息；
8. 空间坐标信息；
9. 五方主体信息。

### 7.2 查询数据子类

在业务数据、服务实例数据或日志数据页面，可按“数据子类”和“关键词”查询汇总行。点击“重置”恢复全部数据。

汇总表显示：

- 数据子类和数据库表名；
- 字段数和已导入记录数；
- 最近上传时间、上传人和文件；
- 预览、合规检查和删除等操作。

### 7.3 新增数据子类

1. 点击“+ 新增数据子类”。
2. 填写数据子类名称。
3. 填写数据库表名；留空时系统自动生成名称。
4. 填写字段数。
5. 点击“保存”。

同一一级类型下已存在同名子类时，系统会更新原配置并重新显示该行。

### 7.4 删除数据子类行

点击汇总表中的“删除行”并确认。

- 内置子类会被隐藏，不会物理删除配置；
- 自定义子类配置会被删除；
- 此操作不会自动删除该子类已经导入的业务记录。

如需删除导入数据，应在“预览”中删除单条记录，或在“上传记录”中删除整个批次。

### 7.5 准备 Excel 文件

建议使用 `.xlsx` 文件，并遵循以下要求：

- 文件不超过 10 MB；
- 第一行为表头，至少再包含一行数据；
- 系统只读取第一个工作表；
- 表头可使用系统定义的中文字段名或英文字段名；
- 每行尽量提供稳定的工程、服务或日志标识字段；
- 避免空白表头和完全空白的数据行。

页面允许选择 `.xls`，但当前后端使用 XLSX 解析器，实际操作应优先使用 `.xlsx`。

### 7.6 导入 Excel

1. 进入正确的数据一级类型。
2. 点击“+ 上传 Excel”。
3. 选择文件。
4. 点击“导入”。
5. 查看导入结果中的识别字段数、扩展字段数、原始行数和生成记录数。

系统会按照表头所属的数据子类拆分每一行，因此“生成数据记录数”可能大于 Excel 的原始行数。

无法匹配标准字典的字段不会丢失，而是保存到记录的 `extra_json` 扩展字段中。导入成功后可点击“查看本次扩展字段列”或“查看本次导入数据”进行核对。

每次导入新数据后，所涉及子类的“合规自检”和“温州数安港合规检查”状态会被重置，需要重新执行。

### 7.7 处理重复文件

系统按“文件内容哈希 + 数据一级类型”检测重复文件。

检测到重复文件时：

1. 系统默认阻止导入，并显示原文件名、首次上传时间和批次号。
2. 点击“查看历史上传记录”可核对原批次。
3. 通常应保留原批次，不再重复上传。

页面会显示“仍然导入一次”按钮，但当前版本打开上传框时会重置导入策略，强制导入不能可靠生效。确实需要保留另一份内容时，应先确认业务必要性，再修改文件内容后重新导入；仅修改文件名不会改变重复检测结果。

### 7.8 预览和删除单条记录

1. 在目标数据子类行点击“预览”。
2. 可输入关键词并点击“搜索”。
3. 使用分页按钮浏览记录。
4. 对含扩展字段的记录点击“查看”。
5. 如需删除记录，点击“删除”并确认。

删除单条记录不会删除其所属上传批次或原始 Excel 文件。

### 7.9 上传记录

点击“上传记录”后，可按文件名查询历史批次。列表展示：

- 批次号和原始文件名；
- 上传人和上传时间；
- Excel 数据行数和生成记录数；
- 识别字段数和扩展字段数；
- 涉及的数据子类。

每个批次支持：

- “扩展字段列”：查看未匹配字段及样例值；
- “数据”：查看该批次生成的记录；
- “下载”：下载原始上传文件；
- “删除”：删除批次及其全部数据库记录。

删除批次属于高风险操作。当前实现会删除数据库中的批次和关联记录，但不会自动清理磁盘上的原始上传文件。

### 7.10 合规检查

每个数据子类提供两级检查。

#### 合规自检

系统检查：

- 数据子类名称是否为空；
- 数据库表名是否为空；
- 字段数是否大于 0；
- 当前子类是否已有导入记录。

无导入记录时会给出提示，但不一定阻止自检通过。

#### 温州数安港合规检查

执行前必须先通过合规自检。系统还会检查数据库表名和字段数。检查通过后，页面显示“数安港检查通过”。

建议顺序：

```text
导入数据 → 核对扩展字段 → 合规自检 → 温州数安港合规检查
```

### 7.11 业务元数据

业务元数据页面根据业务数据字段字典生成字段清单，显示：

- 业务数据子类；
- 数据库表名；
- 字段序号；
- 中文表头字段；
- 数据库字段名；
- 元数据类型。

可按业务数据子类和关键词筛选，并点击“导出字段清单”下载 Excel。该页面不提供 Excel 导入。

### 7.12 导出

在业务数据、服务实例数据或日志数据页面点击“导出”，系统会生成按数据子类分工作表的 `.xlsx` 文件。

业务元数据页面的“导出字段清单”会导出业务字段定义，而不是已导入的业务记录。

## 8. 服务组件管理

### 8.1 服务状态

| 状态 | 含义 |
| --- | --- |
| 草稿 | 服务刚创建，可继续编辑或提交审批 |
| 待审批 | 已提交管理员处理 |
| 已发布 | 服务已发布，可由管理员授权、配置或下架 |
| 已授权 | 已填写授权单位，可由授权部门查看 |
| 已下架 | 服务停止对外提供，可编辑、重新发布或按权限删除 |

标准流转如下：

```text
注册 → 草稿 → 提交审批 → 待审批 → 发布 → 已发布 → 授权 → 已授权
                                                    ↓
                                                  下架
```

当前版本没有单独的“驳回到草稿”按钮。管理员可将待审批服务下架，外部部门再编辑后重新提交。

### 8.2 注册服务

1. 点击“注册新服务”或“提交服务申请”。
2. 填写必填项：服务名称、服务编码和版本。
3. 根据需要填写所属部门、接口地址、安全等级、服务描述和备注。
4. 档案管理员还可填写授权单位。
5. 点击“提交”保存。

保存后状态为“草稿”。外部部门的所属部门由系统自动设置，不能自行修改。

> 外部部门点击“提交服务申请”完成的第一步仅创建草稿，仍需在服务卡片上点击“提交审批”。

### 8.3 编辑和删除服务

- 草稿：创建部门可编辑、提交审批或删除。
- 待审批：创建部门可编辑；仅管理员可发布或下架。
- 已发布：仅管理员可授权、配置或下架。
- 已授权：仅管理员可配置或下架。
- 已下架：创建部门可编辑；管理员可重新发布；创建部门可删除本部门服务。

外部部门不能直接删除待审批、已发布或已授权的服务。

### 8.4 提交审批

在草稿服务卡片上点击“提交审批”，确认后状态变为“待审批”。

提交前应重点检查：

- 服务编码是否唯一、稳定；
- 版本号是否正确；
- 接口地址是否可访问；
- 安全等级是否与数据敏感程度一致；
- 描述是否写明服务用途和返回内容。

### 8.5 发布和下架

档案管理员可对待审批或已下架服务点击“发布”，状态变为“已发布”。

管理员可点击“下架”，将待审批、已发布或已授权服务转为“已下架”。下架前应确认调用方已收到通知。

### 8.6 服务授权

1. 在“已发布”服务上点击“授权”。
2. 输入授权单位；多个单位使用逗号分隔。
3. 点击“确认授权”。

授权成功后，服务状态变为“已授权”。

授权单位名称应与外部账号所属部门完全一致，例如“物业办”“质安科”“不动产”“物业公司”“应急办”或“公安系统”，否则外部部门可能无法看到服务。

### 8.7 技术配置

档案管理员可对已发布或已授权服务点击“配置”，设置：

- 服务路由；
- 认证方式：无需认证、Token、OAuth2 或 API Key；
- 超时时间；
- 失败重试次数；
- 是否启用服务埋点；
- 是否启用访问日志。

点击“保存配置”后，配置以 JSON 形式保存。配置页面只保存参数，不会自动部署真实接口、网关或认证服务。

## 9. 外部部门操作流程

外部部门用户登录后，建议按以下步骤操作：

1. 在服务组件管理页面查看本部门已有申请。
2. 点击“提交服务申请”并填写服务信息。
3. 在新生成的草稿卡片上点击“提交审批”。
4. 等待档案管理员发布或下架。
5. 查看已发布服务，以及明确授权给本部门的已授权服务。
6. 如服务处于草稿、待审批或已下架状态，可按页面按钮和权限继续编辑。
7. 进入“设置”修改本人密码或退出登录。

外部部门无权访问数据导入、内部审批、监控运维和安全防护模块。直接访问相关地址时，系统会跳转回服务组件页面。

## 10. 监控运维

监控运维页面仅对档案管理员开放，主要展示：

- 服务健康度；
- 平均响应时间；
- 今日调用次数；
- 安全事件数；
- 服务调用趋势；
- 服务状态分布；
- 认证服务、数据服务、存储服务和网关服务状态；
- 实时事件日志。

当前页面的“刷新”按钮未绑定独立刷新逻辑，可使用浏览器刷新页面更新显示。

> 当前版本的部分监控数值由固定值、随机值或演示事件生成，只用于界面演示，不能作为生产运行、计费、SLA 或故障判断依据。

## 11. 安全防护

安全防护页面仅对档案管理员开放，展示：

- 身份认证、权限控制、数据加密和日志审计状态；
- 最近 7 天安全事件与威胁检测趋势；
- 威胁类型分布；
- 安全事件的时间、类型、来源 IP、描述、严重程度和处理状态。

> 当前安全事件、趋势和部分安全能力状态属于演示数据。系统当前实际登录方式为用户名和密码，不能仅依据页面上的“多因素认证”“AES-256”等状态认定生产安全措施已经落地。

监控运维和安全防护图表通过互联网 CDN 加载 ECharts。服务器或浏览器无法访问外网时，图表区域可能为空，但其他页面功能不受影响。

## 12. 系统设置

### 12.1 修改密码

1. 进入“设置”。
2. 输入旧密码。
3. 输入新密码。
4. 再次输入新密码。
5. 点击“修改密码”。

两次新密码不一致或旧密码错误时，系统会给出提示。

当前版本未强制密码复杂度。建议密码不少于 12 位，并包含大小写字母、数字和特殊字符。

### 12.2 设置登录背景

此功能仅对档案管理员开放：

1. 在“背景图片设置”中选择图片。
2. 点击“上传背景图片”。
3. 上传成功后，新的背景用于登录页。

建议使用 JPG 或 PNG 图片，文件不超过 10 MB。上传新背景后，系统会删除旧背景文件。

## 13. 推荐日常操作流程

### 13.1 档案管理员

1. 登录后检查首页审批事项。
2. 处理待审批记录并填写明确审批意见。
3. 检查待审批服务，确认信息后发布或下架。
4. 对已发布服务配置路由并填写授权单位。
5. 导入数据要素，核对识别字段和扩展字段。
6. 依次执行合规自检和温州数安港合规检查。
7. 查看上传记录，避免重复批次。
8. 查看监控和安全页面；生产判断应以真实监控、审计平台为准。
9. 完成操作后退出登录。

### 13.2 外部部门

1. 登录后查看本部门服务状态。
2. 新建或编辑服务草稿。
3. 点击“提交审批”。
4. 查看管理员发布和授权结果。
5. 定期修改密码并在完成操作后退出。

## 14. 数据备份与安全注意事项

建议在执行批次删除、大量导入、数据库升级前，同时备份数据库和 `uploads` 目录。

数据库备份示例：

```bash
mysqldump -u root -p collaborative_sharing > collaborative_sharing_backup.sql
```

恢复数据库示例：

```bash
mysql -u root -p collaborative_sharing < collaborative_sharing_backup.sql
```

上传文件位于项目根目录的 `uploads` 下，至少应备份：

- `uploads/data-elements`：数据要素原始 Excel；
- `uploads/todo`：审批附件；
- `uploads/background`：登录背景图片。

安全注意事项：

- 不要在生产环境继续使用默认账号和密码；
- 当前版本密码以明文方式存储，应在生产部署前升级为安全哈希存储；
- 不要将 `application.yml`、数据库备份或 `uploads` 中的敏感文件公开；
- 删除批次、审批或服务前先确认备份；
- 生产环境应在反向代理层启用 HTTPS、访问控制和日志审计。

## 15. 常见问题

### 15.1 无法连接数据库

检查 MySQL 是否启动：

```bash
mysql -u root -p -e "SELECT 1;"
```

确认 `application.yml` 中的地址、端口、用户名和密码与实际数据库一致。

### 15.2 8080 端口被占用

macOS 或 Linux 可执行：

```bash
lsof -i :8080
```

停止占用进程，或修改 `application.yml` 中的 `server.port` 后重新启动。

### 15.3 Excel 导入失败

依次检查：

- 文件是否为有效的 `.xlsx`；
- 文件是否超过 10 MB；
- 第一行是否为表头；
- 是否至少有一行非空数据；
- 目标数据一级类型是否选择正确；
- 文件是否已经导入过；
- 应用是否对项目根目录下的 `uploads` 具有写权限。

### 15.4 Excel 导入后记录数比行数多

这是正常情况。系统会根据表头所属数据子类，将同一 Excel 行拆分成多条数据记录；业务数据中的五方主体信息还可能进一步拆分。

### 15.5 未识别字段是否会丢失

不会。未识别字段和值保存在 `extra_json` 中，可在导入结果、上传记录或数据预览中查看。

### 15.6 外部部门看不到已授权服务

检查服务是否为“已授权”状态，以及“授权单位”中填写的名称是否与账号部门名称一致。多个单位建议使用中文逗号或英文逗号分隔。

### 15.7 外部部门访问首页后被跳转

这是正常的权限控制行为。外部部门登录后仅使用服务组件管理和设置模块。

### 15.8 图表区域为空

监控和安全图表依赖 `cdn.jsdelivr.net`。检查浏览器网络、服务器出口网络和安全策略是否允许加载该资源。

### 15.9 上传文件或背景图片无法访问

确保应用从项目根目录启动，并确认对应文件存在于 `uploads` 下。系统使用启动时的工作目录定位上传文件。

## 16. 常用地址

| 页面 | 地址 |
| --- | --- |
| 登录 | `/login` |
| 首页 | `/` |
| 审批管理 | `/todo-items` |
| 业务数据 | `/data-elements/business` |
| 业务元数据 | `/data-elements/metadata` |
| 服务实例数据 | `/data-elements/service-instance` |
| 日志数据 | `/data-elements/log` |
| 服务组件管理 | `/service-components` |
| 监控运维 | `/monitoring` |
| 安全防护 | `/security` |
| 设置 | `/settings` |
| 退出登录 | `/logout` |

## 17. 当前版本已知限制

- 登录页“自动登录”和“忘记密码”尚未实现。
- Excel 导入实际应使用 `.xlsx`，旧版 `.xls` 兼容性不足。
- 重复文件页面的“仍然导入一次”当前不能可靠启用强制导入。
- 数据要素列表页面上的主要有效筛选项为数据子类和关键词；批次历史可在“上传记录”中按文件名查询。
- 服务审批、内部审批和合规检查之间不会自动联动。
- 服务技术配置只保存配置数据，不会自动创建真实 API 或网关路由。
- 删除上传批次不会自动删除磁盘中的原始 Excel 文件。
- 监控指标、安全事件和部分状态为演示数据。
- 密码未加密存储，不适合未经加固直接用于生产环境。
